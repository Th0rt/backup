<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8">
    <title>タイトル</title>
  </head>
  <body>
    <form action="/" method="POST">
      <p><label>名前: <input type="text" name="target_name"></label></p>
      <p><label>年齢: <input type="text" name="target_age"></label></p>
      <input type="submit" value="送信">
    </form>
    

<% name = params["target_name"] %>
<% age = params["target_age"] %>

<% time_japan = Time.now.getlocal("+09:00") %>

<p>今は <%=time_japan.hour %> 時です。</P>

<% if 6 <= time_japan.hour && time_japan.hour <12 %>
  <p>おはようございます、<%= name %>(<%= age %>)　さん</p>

<% elseif 12 <= time_japan.hour && time_japan.hour <18 %>
  <p>こんにちは、<%= name %>(<%= age %>)　さん</p>

<% else %>
  <p>こんばんは、<%= name %>(<%= age %>)　さん</p>

<% end %>

  </body>
</html>
